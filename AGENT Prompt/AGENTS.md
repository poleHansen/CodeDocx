# 项目开发规范

## 核心原则

始终以**最少的代码实现最优方案**。

优先保证：

* 逻辑清晰
* 架构简单
* 工作流完整闭环
* 易于维护
* 易于理解
* 符合真实使用场景

避免：

* 过度设计
* 冗余抽象
* 无意义封装
* 为未来假设需求预留大量代码
* 多套重复实现方案

---

## 语言要求

* 所有思考和回复必须使用纯中文。
* 禁止中英文混杂。
* 代码、文件路径、命令行输出保持原样。
* 不输出隐藏推理过程或思维链；需要说明原因时，只给出简洁结论和关键依据。

---

## 0. 铁律：需求不确认，禁止开工

本节优先于后续所有规则。

凡涉及创建、修改、删除文件/代码/配置/依赖，或执行可能改变系统状态的命令，都属于“改动性任务”：**必须先确认需求，再动手。**

未收到明确确认之前产生的任何代码、修改、命令执行结果，都视为无效工作。

禁止用“我猜”“我觉得”“通常应该是”“先做出来再改”代替用户拍板。

即使任务看起来很简单，也必须先走一次“需求确认闸门”。

只有纯咨询、解释、回答问题时可以直接回答，不适用确认闸门。

---

## 1. 需求确认闸门

开工前必须一次性输出以下内容，并等待确认：

1. **需求复述**：用两三句话说明要做什么。
2. **假设清单**：所有打算采用的假设，每条后面标注 `（待确认）`。
3. **歧义/备选方案**：如果需求有多种理解，列出选项：
   * 方案一：……（推荐）
   * 方案二：……
4. **执行计划**：
   * 第 1 步：…… → 验证：……
   * 第 2 步：…… → 验证：……

只有收到用户明确回复，例如：

* “确认”
* “按这个计划做”
* “方案一可以”

才允许开工。

如果用户只回复“可以/开始吧”，但计划中仍有未确认项，必须再次指出未确认项并等待回答，不得直接执行。

---

## 2. 提问是义务，不是可选项

* 遇到需求不清楚、信息不够、结果不可验证时，必须停下提问。
* 禁止“先跳过不确定的部分，把确定的部分做掉”。
* 提问要批量，不要挤牙膏式一条条问。
* 每个问题尽量给出推荐选项，降低用户决策成本：
  * “我建议……你同意吗？”
  * “用方案一还是方案二？”
* 如果用户没有对某件事做决定，不得替用户决定。

---

## 3. 极简主义

**用最少的代码解决问题，不做任何多余的事。**

* 绝不实现需求以外的功能。
* 不为一次性逻辑做抽象。
* 不添加未被要求的“灵活性”或“可配置性”。
* 不处理不可能发生的错误场景。
* 如果写了 200 行但 50 行就能解决，立刻重写。

时刻自问：“一个资深工程师会觉得这个设计过度复杂吗？”如果是，马上简化。

---

## 4. 精准修改

**只动必须动的，只清自己留下的。**

* 绝不“顺手”调整无关代码、注释、格式。
* 绝不重构没有问题的代码。
* 严格匹配现有代码风格，即使不认同。
* 发现无关死代码只提一句，不删除。
* 只清理自己改动产生的垃圾；原有死代码除非明确要求，否则不动。
* 检验标准：每一行改动都必须能直接对应到已确认的需求。

---

## 5. 结果导向：先定成功标准，验证后再交付

任务开始前先定义：

* 什么叫“完成”？
* 用什么标准验证“完成”？

模糊任务必须转化为可验证目标：

* “加个校验” → 先定义非法输入，再写校验逻辑，最后验证。
* “修个 bug” → 先复现 bug，再修复，再验证修复。
* “重构代码” → 重构前后现有验证/测试必须全部通过。

多步骤任务按执行计划逐项核对，不能跳过验证点。

交付时报告验证结果，而不是只说“已完成”。

---

## 6. 编码规范

**处理中文内容时必须避免乱码。**

* 使用 PowerShell 前先执行 `chcp 65001 > $null` 切换到 UTF-8 编码。
* 分析中文日志优先使用 `rg` 代替 `Select-String`。
* 输出中文内容时确保终端编码为 UTF-8。

---

## 7. 违规处理：发现擅自开工，立即停止

如果没有先确认需求就动了手，属于违规，必须立即停止，不能“顺便做完”。

停止后向用户说明：

* 目前已经做了什么；
* 改了哪些文件/命令；
* 为什么这是不合规的；
* 下一步需要用户怎么决定。

等用户明确决定继续、回滚或重新规划后，再行动。

---

## 8. 方案设计规范

设计方案时必须：

1. 优先选择最简单可行方案
2. 优先减少系统复杂度
3. 优先减少依赖数量
4. 优先减少维护成本
5. 优先保证长期可迭代性

要求：

* 一个问题只保留一个最佳方案
* 避免多个并行实现
* 避免大量兜底逻辑
* 避免同一功能出现多套实现路径
* 保证流程天然闭环

---

## 9. Bug 处理规范

发现问题时：

### 禁止

* 猜测原因
* 经验判断
* 通过增加补丁掩盖问题
* 堆叠防御性代码

### 必须

1. 阅读当前真实代码
2. 分析实际执行流程
3. 定位准确根因
4. 验证根因是否成立
5. 给出最小修复方案

### 修复要求

* 直接解决根因
* 不引入额外复杂度
* 不增加无意义兼容逻辑
* 不增加重复状态管理
* 不增加临时补丁代码

---

## 10. 代码实现规范

代码必须：

* 简洁
* 清晰
* 可维护
* 高内聚
* 低耦合

要求：

* 删除无用代码
* 删除废弃逻辑
* 删除重复实现
* 删除历史遗留兼容代码

禁止：

* Copy-Paste 实现
* 重复工具函数
* 重复状态维护
* 无意义包装层
* 无意义配置项

### 新增实现时

必须检查：

1. 旧实现是否还能保留
2. 新实现是否可以直接替代旧实现
3. 如果可以替代，则直接移除旧实现

目标：

> 保持系统持续收敛，而不是持续堆积代码。

---

## 11. 迭代维护规范

每次迭代必须：

1. 新增功能
2. 清理历史遗留代码
3. 清理废弃接口
4. 清理无效配置
5. 清理无用依赖

目标：

> 系统随着迭代越来越简单，而不是越来越复杂。

---

## 12. 第三方库使用规范

使用第三方库前必须：

1. 阅读当前最新源码
2. 阅读当前版本文档
3. 阅读实际实现逻辑
4. 阅读公开 API

禁止：

* 根据旧文章判断
* 根据博客判断
* 根据历史经验判断
* 根据过时文档判断

分析结论必须基于：

* 当前版本源码
* 当前版本文档
* 当前版本实现

不得进行幻想式推测。

---

## 13. 代码分析规范

分析项目时必须优先依据：

1. 当前仓库最新代码
2. 当前实际实现
3. 当前调用链
4. 当前运行逻辑

禁止：

* 根据 README 猜实现
* 根据项目简介猜实现
* 根据历史版本猜实现
* 根据同类项目猜实现

所有结论必须能够对应到**实际代码位置**。

---

## 14. 架构规范

架构设计要求：

* 模块职责单一
* 模块边界清晰
* 调用关系简单
* 数据流明确

避免：

* 过度模块化
* 过度抽象
* 多层转发
* 无意义中间层

优先级：

> **简单架构 > 复杂架构**

> **可维护性 > 理论完美性**

---

## 15. UI 规范

UI 设计要求：

* 风格统一
* 间距统一
* 命名统一
* 组件统一

优先：

* 简洁
* 清晰
* 易操作
* 符合平台规范

避免：

* 风格混乱
* 重复组件
* 过度动画
* 无意义视觉效果

---

## 16. 真实场景原则

所有实现必须符合真实使用场景。

禁止：

* 为极低概率情况增加大量代码
* 为理论风险增加复杂逻辑
* 为假设场景增加实现

优先解决：

* 用户真实会遇到的问题
* 线上真实出现的问题
* 当前业务实际需求

---

## 17. 输出要求

提供方案时：

* 直接给出最佳方案
* 明确说明原因
* 明确说明取舍
* 明确说明影响范围

避免：

* 模糊表达
* 不确定结论
* 罗列大量无关选项

如果能够确定最佳方案，则直接推荐**最佳方案**。

---

## 18. Git 提交

* 不用提交 git，用户自己提交 git。

---

## 总体原则

始终遵循：

> **最少代码、最小复杂度、最佳维护性、真实场景优先、根因驱动修复、持续合理收敛架构。**

> **需求没有闭环之前，工作不开始。宁可多问一句，也不要出错后返工。**
